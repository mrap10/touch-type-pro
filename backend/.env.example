PORT=4000
ALLOWED_ORIGINS=http://localhost:3000
JWT_SECRET=your-jwt-secret-here

DATABASE_URL="your-database-connection-string-here"
DIRECT_URL="your-direct-database-connection-string-here"
# Note: The project uses Supabase, so it's recommended to:
# 1. Create a project at https://supabase.com
# 2. Get the connection strings from Settings > Database
# 3. Use "Transaction Pooler" connection string for DATABASE_URL
# 4. Use "Session Pooler" connection string for DIRECT_URL